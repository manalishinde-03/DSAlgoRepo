package pageObjects;

import static org.testng.Assert.assertEquals;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class HomePage {
	
	/*
	 * WebDriver driver;
	 * 
	 * By txt_username_login = By.xpath("//input[@name='username']"); By
	 * txt_password_login = By.xpath("//input[@name='password']"); By btn_login =
	 * By.xpath("//input[@value='Login']");
	 * 
	 * public HomePage(WebDriver driver) {
	 * 
	 * this.driver=driver; }
	 * 
	 * public void validateMsgAfterLogin(String message) { String actualMsg =
	 * driver.findElement(By.xpath("//div[@class='alert alert-primary']")).getText()
	 * ;
	 * 
	 * assertEquals(actualMsg,message); System.out.println("Login Successful!"); }
	 * 
	 * public void verifyUsernameSignOutLinkOnHomePage(String username) { String
	 * actualUserName = driver.findElement(By.xpath("//ul/a[2]")).getText(); String
	 * usernameLowerCase = actualUserName.toLowerCase();
	 * 
	 * assertEquals( usernameLowerCase,username);
	 * System.out.println("Username is displayed on Home page");
	 * 
	 * if(driver.findElement(By.xpath("//ul/a[3]")).isDisplayed()) {
	 * System.out.println("Sign out link is displayed"); } else {
	 * System.out.println("Sign out link is NOT displayed"); } }
	 */
	
	


}
